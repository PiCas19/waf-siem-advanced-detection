# .env.template for the API (professional, documented)
# --------------------------------------------------
# Copy this file to `.env` in the `api/` directory and customize values for
# your environment. Do NOT commit your real `.env` file with secrets to VCS.
# Use a secrets manager or environment provider for production deployments.
#
# Format: KEY=VALUE
# Lines starting with `#` are comments and ignored.

# ---------------------------------------------------------------------------
# Database configuration
# ---------------------------------------------------------------------------
# DATABASE_URL determines where the API stores data. For development, a
# simple SQLite file works well. For production you can use a Postgres or
# MySQL connection string. Examples:
#  - SQLite (default development): DATABASE_URL=./data/waf.db
#  - Postgres: DATABASE_URL=postgres://user:pass@localhost:5432/wafdb?sslmode=disable
# NOTE: When using SQLite ensure the process has write permissions to the file
DATABASE_URL=./data/waf.db

# Application port the API listens on
PORT=8081

# ---------------------------------------------------------------------------
# Email (SMTP) - used for invite/reset emails
# ---------------------------------------------------------------------------
# If you want the app to send real emails, configure an SMTP server here.
# Example for MailHog (development):
#   SMTP_HOST=localhost
#   SMTP_PORT=1025
#   SMTP_USER=
#   SMTP_PASS=
# MailHog web UI: http://localhost:8025

SMTP_HOST=localhost
SMTP_PORT=1025
# Optional credentials for authenticated SMTP servers
SMTP_USER=
SMTP_PASS=

# Address used as the From header for system emails
NO_REPLY_EMAIL=no-reply@example.test

# Public URL of the web UI / site. Used to generate links inside emails
SITE_URL=http://localhost:5173

# ---------------------------------------------------------------------------
# Optional third-party service keys
# ---------------------------------------------------------------------------
# MaxMind license key (if you want to download/update MaxMind GeoIP database)
# If empty, the server will use internal fallback IP ranges.
MAXMIND_LICENSE_KEY=

# ---------------------------------------------------------------------------
# Logging / runtime
# ---------------------------------------------------------------------------
# LOG_LEVEL: trace|debug|info|warn|error
LOG_LEVEL=info

# Any other environment-specific flags or config can be added here.
# ---------------------------------------------------------------------------

# Security reminder:
# - Never commit real secrets to git.
# - In production, prefer provisioning these values via your deployment
#   environment (systemd Environment=, Docker secrets, Kubernetes Secrets,
#   or a secrets manager).
