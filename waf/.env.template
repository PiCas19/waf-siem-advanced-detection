# .env.template for the WAF (Caddy module)
# --------------------------------------------------
# Copy this file to `.env` in the `waf/` directory and customize values for
# your environment. Do NOT commit your real `.env` file with secrets to VCS.
#
# Format: KEY=VALUE
# Lines starting with `#` are comments and ignored.

# ---------------------------------------------------------------------------
# Cloudflare Turnstile configuration (free, unlimited, no tracking)
# ---------------------------------------------------------------------------
# Turnstile Site Key (public, used in HTML forms)
# Get this from: https://dash.cloudflare.com/ → Turnstile
TURNSTILE_SITE_KEY=0x4AAAAAAB_vC04yTw3CJIFZ

# Turnstile Secret Key (private, used for backend verification)
# KEEP THIS SECRET! Never expose in frontend code or commit to public repositories
TURNSTILE_SECRET_KEY=0x4AAAAAAB_vC3CDfh51vS4SmtGZp_M0xu0

# ---------------------------------------------------------------------------
# Security reminder:
# - Never commit real secrets to git.
# - In production, prefer provisioning these values via your deployment
#   environment (systemd Environment=, Docker secrets, Kubernetes Secrets,
#   or a secrets manager).
